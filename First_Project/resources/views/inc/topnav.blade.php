<div class="shadow navbar navbar-expand-lg">
        <a href="{{route('home')}}" class="btn btn-link">Home</a>
        <a href="{{url('/about')}}" class="btn btn-link">About Us</a>
        <a href="{{url('/products')}}" class="btn btn-link">Products</a>
        <a href="{{url('/add')}}" class="btn btn-link">Add Product</a>
        <a href="{{url('/contact')}}" class="btn btn-link">Contact Us</a>
</div>